/**
 * Ember Forge Feature
 * 
 * A real-time leaderboard system for ranking agents, with SSE-based updates.
 * 
 * Note: This module has been moved from the root features directory.
 * The component implementations will need to be migrated in a future task.
 */

// Export components (commented out until implementation is migrated)
// export * from './components/ForgeLeaderboard';
// export * from './components/ForgePage';

// Export API (commented out until implementation is migrated)
// export * from './api/forgeService';

// Export hooks (commented out until implementation is migrated)
// export * from './hooks/useForgeLeaderboardStream';

// Default placeholder export with information about the migration status
const forgePlaceholder = {
  status: 'migrated-structure',
  message: 'Forge feature directory structure has been migrated, but implementation files need to be added in a future task.',
  originalLocation: 'frontend/features/forge',
  targetLocation: 'frontend/app/features/forge',
};

export default forgePlaceholder;