/**
 * Interfaces Index
 * 
 * Exports all interface modules for the Agentic SOC system.
 * Currently includes the natural language interface.
 */

const naturalLanguage = require('./natural_language');

module.exports = {
    // Natural language processing interface
    naturalLanguage,
};